# ==============================================================================
# NETKNIFE - TERRAFORM VARIABLES (DEVELOPMENT)
# ==============================================================================
# Copy this file to terraform.tfvars and fill in your values.
# terraform.tfvars is gitignored and should NEVER be committed.
#
# Before running terraform:
#   export CLOUDFLARE_API_TOKEN="your-cloudflare-api-token"
# ==============================================================================

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# S3 bucket name for static site hosting (must be globally unique)
# Tip: Include your AWS account ID to ensure uniqueness
# Get account ID: aws sts get-caller-identity --query Account --output text
site_bucket_name = "netknife-site-YOUR_ACCOUNT_ID"

# Email for monitoring alerts (budget warnings, Lambda errors, etc.)
# YOU MUST CONFIRM SNS SUBSCRIPTION after deployment!
alert_email = "your-email@example.com"

# ============================================================================
# AWS CONFIGURATION
# ============================================================================

aws_region = "us-west-2"

# ============================================================================
# CUSTOM DOMAIN CONFIGURATION (Optional)
# ============================================================================
# Skip this section to use the CloudFront domain (*.cloudfront.net)
#
# To use a custom domain like tools.alexflux.com:
# 1. Set custom_domain to your full domain
# 2. Set cloudflare_zone_id to your Cloudflare zone ID
# 3. Set cloudflare_api_token (or run ./sync-cloudflare-token.sh to auto-sync)

# Custom domain for the site (leave empty to use CloudFront domain)
custom_domain = "tools.alexflux.com"

# Cloudflare Zone ID (required if using custom_domain)
# Find at: https://dash.cloudflare.com → Domain → API section → Zone ID
cloudflare_zone_id = "YOUR_CLOUDFLARE_ZONE_ID"

# Cloudflare Zone Name (required if using custom_domain)
# The root domain name managed by Cloudflare (e.g., "alexflux.com")
cloudflare_zone_name = "alexflux.com"

# Cloudflare Subdomain (required if using custom_domain)
# The subdomain part (e.g., "tools" creates tools.alexflux.com)
# Should match the subdomain in custom_domain above
cloudflare_subdomain = "tools"

# Cloudflare API Token (required if using custom_domain)
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Or run ./sync-cloudflare-token.sh to automatically sync from openarena project
cloudflare_api_token = "YOUR_CLOUDFLARE_API_TOKEN"

# ============================================================================
# COST CONFIGURATION
# ============================================================================

# Monthly budget limit in USD (alerts at 50%, 80%, 100%)
monthly_budget_usd = 25

# ============================================================================
# THIRD-PARTY API KEYS (Optional)
# ============================================================================
# All API keys are optional. Leave empty to disable that integration.
# Tools without valid API keys will show as disabled in the frontend.

# AbuseIPDB API Key (for IP reputation checks)
# Get a free API key at: https://www.abuseipdb.com/account/api
# Free tier: 1,000 checks per day
abuseipdb_api_key = ""

# Shodan API Key (for internet-connected device search)
# Get at: https://account.shodan.io
# Free tier: Limited searches, membership recommended for full access
shodan_api_key = ""

# VirusTotal API Key (for file/URL/domain/IP analysis)
# Get a free API key at: https://www.virustotal.com/gui/my-apikey
# Free tier: 500 requests/day, 4 requests/minute
virustotal_api_key = ""

# SecurityTrails API Key (for historical DNS/WHOIS data)
# Get at: https://securitytrails.com/app/account/credentials
# Free tier: 50 API calls/month
securitytrails_api_key = ""

# Censys API Key (for internet-wide scan data)
# Get at: https://censys.io/account/api
# Format: "id:secret" (both parts together)
# Free tier: 250 queries/month
censys_api_key = ""

# GreyNoise API Key (for IP threat intelligence)
# Get at: https://viz.greynoise.io/account/api-key
# Note: GreyNoise may require a paid plan - check current pricing
# Leave empty to disable GreyNoise integration
greynoise_api_key = ""

# EmailRep.io API Key (for email reputation checks)
# Get at: https://emailrep.io/
# Free tier: 10,000 queries/month
# Optional: Leave empty to use without API key (lower rate limits)
emailrep_api_key = ""

# NumLookup API Key (for phone number validation)
# Get at: https://numlookupapi.com/
# Free tier available
# Optional: Leave empty to use without API key (lower rate limits)
numlookup_api_key = ""

# IPQualityScore API Key (for IP reputation and fraud detection)
# Get at: https://www.ipqualityscore.com/
# Free tier: 100 requests/day
# Required: Leave empty to disable IPQualityScore integration
ipqualityscore_api_key = ""

# Hunter.io API Key (for email verification and finder)
# Get at: https://hunter.io/api-documentation
# Free tier: 25 requests/month
# Required: Leave empty to disable Hunter.io integration
hunter_api_key = ""

# OpenAI API Key (for Security Advisor AI chatbot)
# Get at: https://platform.openai.com/api-keys
# Pricing: GPT-4o-mini is ~$0.15/$0.60 per 1M tokens (input/output)
# Recommended: GPT-4o-mini (cost-effective, high quality)
# Required: Leave empty to disable Security Advisor chatbot
openai_api_key = ""

# OpenAI Model (optional, defaults to gpt-4o-mini)
# Options: gpt-4o-mini (recommended, cost-effective), gpt-4o (higher quality, more expensive)
# GPT-4o-mini is ~100x cheaper than legacy GPT-4 and provides excellent quality
openai_model = "gpt-4o-mini"

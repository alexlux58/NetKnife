# NetKnife Environment Variables
# Copy this to .env and fill in your values
# .env is gitignored and should NEVER be committed

# Cloudflare API Token (required for custom domain)
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Or the deployment scripts will automatically source from openarena project
CLOUDFLARE_API_TOKEN=""

# Note: If CLOUDFLARE_API_TOKEN is empty, the deployment scripts will try to
# automatically load it from ../../../../openarena-aws/.env if that file exists
